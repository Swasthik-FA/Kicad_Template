# KiCad backup folders
*-backups/
backups/

# KiCad autosave files (start with '#', end with '#')
# Example: #auto_saved_files#
# KiCad uses '#' around autosave filenames
# The following pattern ignores all files with '#' at start or end
# This covers both files and folders
# Note: Git ignores '#' at start of line, so escape it
\#*#
*#*

# KiCad autosave PCB & schematic backup
*_autosave*.kicad_pcb
*_autosave*.kicad_sch

# KiCad old backup formats
*.kicad_pcb-bak
*.kicad_sch-bak

# KiCad autosave files starting with "_autosave"
_autosave-*
